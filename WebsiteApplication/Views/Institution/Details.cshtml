@model Domain.Institution

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Institution</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.InstitutionEndpointAddress)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.InstitutionEndpointAddress)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.InstitutionId }) |
    @Html.ActionLink("Back to List", "Index")
</p>
